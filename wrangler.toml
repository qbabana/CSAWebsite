name = "csa-website"
compatibility_date = "2024-01-01"

# Static assets only (no Worker script) — serves index.html, script.js, style.css
[assets]
directory = "."
not_found_handling = "single-page-application"
